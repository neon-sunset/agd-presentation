# Copy to .env for local docker-compose runs.
# These are bootstrap admin creds for Keycloak's master realm — they let
# you sign into the admin console at http://localhost:8080/admin.
# They are NOT used by the demo flow itself.

KC_BOOTSTRAP_ADMIN_USERNAME=admin
KC_BOOTSTRAP_ADMIN_PASSWORD=change-me-locally
